# BasicMatchFlow

main: kit: flow: BasicMatchFlow:
{
  entity: "match",
  kind: "basic",
  name: "BasicMatchFlow",
  step: [
    {
      match: {
        competition_id: "competition01"
      }

      op: "list",
      valid: [
        {
          apply: "ItemExists",
          def: {
            ref: "match_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicMatchFlow"
}